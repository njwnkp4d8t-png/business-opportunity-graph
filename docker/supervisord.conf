[supervisord]
nodaemon=true
user=appuser
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisor/supervisord.pid
childlogdir=/var/log/supervisor
loglevel=info

[unix_http_server]
file=/var/run/supervisor/supervisor.sock
chmod=0700
chown=appuser:appuser

[supervisorctl]
serverurl=unix:///var/run/supervisor/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[program:nginx]
command=/usr/sbin/nginx -g "daemon off;"
autostart=true
autorestart=true
stdout_logfile=/var/log/nginx/stdout.log
stderr_logfile=/var/log/nginx/stderr.log
stdout_logfile_maxbytes=10MB
stderr_logfile_maxbytes=10MB
priority=10
startsecs=5
startretries=3
stopwaitsecs=10
stopasgroup=true
killasgroup=true

# Uncomment the following section if you have a Python backend service
# [program:backend]
# command=/opt/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
# directory=/app
# autostart=true
# autorestart=true
# stdout_logfile=/var/log/supervisor/backend-stdout.log
# stderr_logfile=/var/log/supervisor/backend-stderr.log
# stdout_logfile_maxbytes=10MB
# stderr_logfile_maxbytes=10MB
# environment=PATH="/opt/venv/bin:%(ENV_PATH)s"
# priority=20
# startsecs=10
# startretries=3
# stopwaitsecs=10
# stopasgroup=true
# killasgroup=true
